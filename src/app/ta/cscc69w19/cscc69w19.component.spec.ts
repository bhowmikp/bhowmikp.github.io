import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { Cscc69w19Component } from './cscc69w19.component';

describe('Cscc69w19Component', () => {
  let component: Cscc69w19Component;
  let fixture: ComponentFixture<Cscc69w19Component>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ Cscc69w19Component ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(Cscc69w19Component);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
